---
/**
 * Internal sprint roadmap. Bilingual: RU is server-rendered default,
 * EN applied client-side via data-ru/data-en attributes (single DOM, no dupes).
 * noindex + excluded from sitemap + disallowed in robots.txt.
 */
import Base from '../layouts/Base.astro';
import {
  UI,
  WEEKS,
  DAYS,
  ROWS,
  CHECKPOINTS,
  FLOW,
  RULES,
  LEGEND,
  type Lang,
} from '../data/roadmap';

const t = (key: string, lang: Lang = 'ru') => UI[key]?.[lang] ?? '';

const cpByDay = new Map(CHECKPOINTS.map((c) => [c.day, c]));
const year = new Date().getFullYear();

const rawBase = import.meta.env.BASE_URL;
const base = rawBase.endsWith('/') ? rawBase : rawBase + '/';
---

<Base
  title="Дорожная карта · Taratorkin Official"
  description="Внутренняя методика спринта студии Taratorkin Official."
  path="/roadmap"
  lang="ru"
  noindex
  bare
>
  <div class="rm container">
    <!-- hero -->
    <header class="rm-hero">
      <div>
        <p class="eyebrow eyebrow--accent" data-ru={t('eyebrow', 'ru')} data-en={t('eyebrow', 'en')}>
          {t('eyebrow', 'ru')}
        </p>
        <h1 class="rm-hero__title display" data-ru={t('title', 'ru')} data-en={t('title', 'en')}>
          {t('title', 'ru')}
        </h1>
        <p class="rm-hero__lede" data-ru={t('lede', 'ru')} data-en={t('lede', 'en')}>
          {t('lede', 'ru')}
        </p>
      </div>
      <div class="rm-hero__side">
        <div class="lang-toggle" role="group" aria-label="Язык / Language">
          <button type="button" data-lang="ru" class="is-active" aria-pressed="true">RU</button>
          <button type="button" data-lang="en" aria-pressed="false">EN</button>
        </div>
        <a href={base} class="rm-home mono">← taratorkinofficial.github.io</a>
        <dl class="rm-stats">
          <div><dd class="mono">20</dd><dt data-ru={t('statDays','ru')} data-en={t('statDays','en')}>{t('statDays','ru')}</dt></div>
          <div><dd class="mono">4</dd><dt data-ru={t('statWeeks','ru')} data-en={t('statWeeks','en')}>{t('statWeeks','ru')}</dt></div>
          <div><dd class="mono">6</dd><dt data-ru={t('statCheckpoints','ru')} data-en={t('statCheckpoints','en')}>{t('statCheckpoints','ru')}</dt></div>
        </dl>
      </div>
    </header>

    <!-- timeline board -->
    <p class="rm-scroll-hint mono" data-ru={t('scrollHint','ru')} data-en={t('scrollHint','en')}>
      {t('scrollHint', 'ru')}
    </p>
    <div class="board-scroll">
      <div class="board">
        <!-- week tabs -->
        <div class="board__weeks">
          <div></div>
          {
            WEEKS.map((w) => (
              <div class="wk" style={`--wc: var(${w.colorVar})`}>
                <span class="wk__num mono" data-ru={w.num.ru} data-en={w.num.en}>{w.num.ru}</span>
                <span class="wk__title" data-ru={w.title.ru} data-en={w.title.en}>{w.title.ru}</span>
              </div>
            ))
          }
        </div>

        <!-- day headers -->
        <div class="board__days">
          <div></div>
          {
            DAYS.ru.map(([wd, num], i) => (
              <div class:list={['day', { 'day--wb': i > 0 && i % 5 === 0 }]}>
                <span data-ru={wd} data-en={DAYS.en[i][0]}>{wd}</span>
                <span class="day__num mono">{num}</span>
              </div>
            ))
          }
        </div>

        <!-- calendar rows -->
        <div class="board__cal">
          {
            ROWS.map((row) => (
              <>
                <div class="role" data-role={row.key}>
                  <span class="role__tag mono">
                    {row.tag} · <span data-ru={t('roleWord','ru')} data-en={t('roleWord','en')}>{t('roleWord','ru')}</span>
                  </span>
                  <span class="role__name" data-ru={row.name.ru} data-en={row.name.en}>{row.name.ru}</span>
                  <span class="role__sub" data-ru={row.sub.ru} data-en={row.sub.en}>{row.sub.ru}</span>
                </div>
                {row.items.map((it) => (
                  <div
                    class:list={['cell', `cell--${it.type}`, { 'cell--strong': it.strong }]}
                    style={`grid-column: span ${it.span}`}
                  >
                    <span class="cell__title" data-ru={it.title.ru} data-en={it.title.en}>{it.title.ru}</span>
                    {it.sub && (
                      <span class="cell__sub" data-ru={it.sub.ru} data-en={it.sub.en}>{it.sub.ru}</span>
                    )}
                  </div>
                ))}
              </>
            ))
          }
        </div>

        <!-- checkpoints -->
        <div class="board__cps">
          <div class="cps__label mono" data-ru={t('checkpointsLabel','ru')} data-en={t('checkpointsLabel','en')}>
            {t('checkpointsLabel', 'ru')}
          </div>
          {
            Array.from({ length: 20 }, (_, i) => i + 1).map((d) => {
              const cp = cpByDay.get(d);
              return (
                <div class="cps__cell">
                  {cp && (
                    <div class:list={['cp', { 'cp--release': cp.release }]}>
                      <span class="cp__dot" />
                      <span class="cp__label mono">
                        <span data-ru={cp.text.ru} data-en={cp.text.en}>{cp.text.ru}</span>{' '}
                        <span class="cp__day" data-ru={cp.dayLabel.ru} data-en={cp.dayLabel.en}>{cp.dayLabel.ru}</span>
                      </span>
                    </div>
                  )}
                </div>
              );
            })
          }
        </div>
      </div>
    </div>

    <!-- legend -->
    <ul class="legend" aria-label="Легенда">
      {
        LEGEND.map((l) => (
          <li class="legend__chip" data-type={l.type}>
            <span class="legend__dot" />
            <span data-ru={l.label.ru} data-en={l.label.en}>{l.label.ru}</span>
          </li>
        ))
      }
    </ul>

    <!-- artist flow -->
    <section class="rm-sec">
      <div class="rm-sec__head">
        <span class="mono rm-sec__idx" data-ru={t('flowIdx','ru')} data-en={t('flowIdx','en')}>{t('flowIdx','ru')}</span>
        <h2 class="display">
          <span data-ru={t('flowTitleA','ru')} data-en={t('flowTitleA','en')}>{t('flowTitleA','ru')}</span>
          <em data-ru={t('flowTitleB','ru')} data-en={t('flowTitleB','en')}>{t('flowTitleB','ru')}</em>
        </h2>
      </div>
      <ol class="flow">
        {
          FLOW.map((f, i) => (
            <li class="flow__step">
              <span class="flow__num mono">{i + 1}</span>
              <span class="flow__lbl" data-ru={f.lbl.ru} data-en={f.lbl.en}>{f.lbl.ru}</span>
              <span class="flow__desc" data-ru={f.desc.ru} data-en={f.desc.en}>{f.desc.ru}</span>
            </li>
          ))
        }
      </ol>
      <p class="ios-note">
        <span class="ios-note__badge mono">iOS QA</span>
        <span>
          <strong data-ru={t('iosStrong','ru')} data-en={t('iosStrong','en')}>{t('iosStrong','ru')}</strong><span data-ru={t('iosText','ru')} data-en={t('iosText','en')}>{t('iosText','ru')}</span>
        </span>
      </p>
    </section>

    <!-- rules -->
    <section class="rm-sec">
      <div class="rm-sec__head">
        <span class="mono rm-sec__idx" data-ru={t('rulesIdx','ru')} data-en={t('rulesIdx','en')}>{t('rulesIdx','ru')}</span>
        <h2 class="display">
          <span data-ru={t('rulesTitleA','ru')} data-en={t('rulesTitleA','en')}>{t('rulesTitleA','ru')}</span>
          <em data-ru={t('rulesTitleB','ru')} data-en={t('rulesTitleB','en')}>{t('rulesTitleB','ru')}</em>
        </h2>
      </div>
      <div class="rules">
        {
          RULES.map((r) => (
            <article class="rule">
              <span class="mono rule__num">{r.num}</span>
              <h3 data-ru={r.h3.ru} data-en={r.h3.en}>{r.h3.ru}</h3>
              <p data-ru={r.p.ru} data-en={r.p.en}>{r.p.ru}</p>
            </article>
          ))
        }
      </div>
    </section>

    <footer class="rm-footer mono">
      <span data-ru={`${t('footerMain','ru')} · ${year}`} data-en={`${t('footerMain','en')} · ${year}`}>
        {t('footerMain', 'ru')} · {year}
      </span>
      <span class="rm-footer__sig" data-ru={t('footerSig','ru')} data-en={t('footerSig','en')}>
        {t('footerSig', 'ru')}
      </span>
    </footer>
  </div>
</Base>

<style>
  .rm {
    padding-block: clamp(32px, 5vw, 64px);
  }

  /* hero */
  .rm-hero {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: 32px;
    border: 1px solid var(--line-strong);
    background: var(--surface);
    padding: clamp(24px, 4vw, 44px);
    margin-bottom: 36px;
    box-shadow: 8px 8px 0 var(--line-strong);
  }

  .rm-hero__title {
    font-size: clamp(30px, 5vw, 56px);
    margin-top: 16px;
  }

  .rm-hero__lede {
    max-width: 52ch;
    color: var(--ink-2);
    margin-top: 14px;
  }

  .rm-hero__side {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 16px;
    flex-shrink: 0;
  }

  .lang-toggle {
    display: inline-flex;
    border: 1px solid var(--line-strong);
  }

  .lang-toggle button {
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.14em;
    padding: 8px 16px;
    color: var(--ink-2);
  }

  .lang-toggle button.is-active {
    background: var(--ink);
    color: var(--paper);
  }

  .rm-home {
    font-size: 11px;
    color: var(--ink-3);
    text-decoration: none;
  }

  .rm-home:hover {
    color: var(--accent-ink);
  }

  .rm-stats {
    display: flex;
    gap: 10px;
  }

  .rm-stats div {
    border: 1px solid var(--line);
    padding: 12px 18px;
    text-align: center;
  }

  .rm-stats dd {
    font-size: 26px;
    font-weight: 700;
    color: var(--accent-ink);
    line-height: 1;
  }

  .rm-stats dt {
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--ink-3);
    margin-top: 6px;
  }

  @media (max-width: 900px) {
    .rm-hero {
      flex-direction: column;
      align-items: stretch;
    }

    .rm-hero__side {
      align-items: flex-start;
    }
  }

  /* board */
  .rm-scroll-hint {
    display: none;
    font-size: 10px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--ink-3);
    margin-bottom: 10px;
  }

  .board-scroll {
    overflow-x: auto;
    padding-bottom: 12px;
  }

  .board {
    min-width: 1500px;
  }

  @media (max-width: 1560px) {
    .rm-scroll-hint {
      display: block;
    }
  }

  .board__weeks,
  .board__days,
  .board__cal,
  .board__cps {
    display: grid;
    grid-template-columns: 130px repeat(20, minmax(56px, 1fr));
    gap: 4px;
  }

  .board__weeks {
    margin-bottom: 12px;
  }

  .wk {
    grid-column: span 5;
    border: 1px solid var(--line-strong);
    border-top: 3px solid var(--wc);
    background: var(--surface);
    padding: 12px 16px;
  }

  .wk__num {
    display: block;
    font-size: 10px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--wc);
  }

  .wk__title {
    display: block;
    font-weight: 700;
    font-size: 14px;
    margin-top: 3px;
  }

  .board__days {
    margin-bottom: 8px;
  }

  .day {
    text-align: center;
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--ink-3);
    padding-block: 6px;
    position: relative;
  }

  .day__num {
    display: block;
    font-size: 13px;
    color: var(--ink-2);
    font-weight: 700;
  }

  .day--wb::before {
    content: '';
    position: absolute;
    left: -2px;
    top: -8px;
    bottom: -8px;
    width: 1px;
    background: var(--line-strong);
    opacity: 0.4;
  }

  .role {
    border: 1px solid var(--line-strong);
    border-left: 3px solid var(--rc, var(--ink-3));
    background: var(--surface);
    padding: 12px 10px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .role[data-role='founder'] { --rc: var(--ph-polish); }
  .role[data-role='lead'] { --rc: var(--ph-dev); }
  .role[data-role='third'] { --rc: var(--ph-assets); }
  .role[data-role='artist'] { --rc: var(--ph-rel); }

  .role__tag {
    font-size: 9.5px;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--ink-3);
  }

  .role__name {
    font-weight: 700;
    font-size: 14px;
    margin-top: 3px;
  }

  .role__sub {
    font-size: 11.5px;
    color: var(--ink-2);
  }

  .cell {
    border: 1px solid color-mix(in srgb, var(--cc) 45%, transparent);
    background: color-mix(in srgb, var(--cc) 9%, var(--surface));
    min-height: 72px;
    padding: 8px 5px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    text-align: center;
  }

  .cell--concept { --cc: var(--ph-concept); }
  .cell--assets { --cc: var(--ph-assets); }
  .cell--dev { --cc: var(--ph-dev); }
  .cell--polish { --cc: var(--ph-polish); }
  .cell--mkt { --cc: var(--ph-mkt); }
  .cell--rel { --cc: var(--ph-rel); }
  .cell--buf { --cc: var(--ph-buf); border-style: dashed; }

  .cell--strong {
    border-width: 2px;
    border-color: var(--cc);
  }

  .cell__title {
    font-size: 12px;
    font-weight: 700;
    line-height: 1.25;
    overflow-wrap: break-word;
  }

  .cell--strong .cell__title {
    color: var(--cc);
  }

  .cell__sub {
    font-size: 10.5px;
    color: var(--ink-2);
    margin-top: 3px;
    line-height: 1.3;
  }

  /* checkpoints */
  .board__cps {
    margin-top: 20px;
    padding-block: 20px 44px;
    position: relative;
  }

  .board__cps::before {
    content: '';
    position: absolute;
    left: 134px;
    right: 0;
    top: 32px;
    height: 1px;
    background: var(--line-strong);
    opacity: 0.35;
  }

  .cps__label {
    font-size: 11px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--ink-3);
    align-self: center;
  }

  .cps__cell {
    position: relative;
    display: flex;
    justify-content: center;
    min-height: 26px;
  }

  .cp {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .cp__dot {
    width: 12px;
    height: 12px;
    background: var(--ph-buf);
    border: 2px solid var(--paper);
    outline: 1px solid var(--ph-buf);
    z-index: 1;
  }

  .cp--release .cp__dot {
    background: var(--ph-polish);
    outline-color: var(--ph-polish);
  }

  .cp__label {
    position: absolute;
    top: 20px;
    white-space: nowrap;
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.06em;
    padding: 4px 8px;
    border: 1px solid var(--ph-buf);
    background: var(--paper);
    color: var(--ph-buf);
  }

  .cp--release .cp__label {
    border-color: var(--ph-polish);
    color: var(--ph-polish);
    transform: translateX(-24px);
  }

  .cp__day {
    opacity: 0.65;
    font-weight: 400;
  }

  /* legend */
  .legend {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 28px;
    padding-top: 20px;
    border-top: 1px solid var(--line);
  }

  .legend__chip {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 12.5px;
    color: var(--ink-2);
    border: 1px solid var(--line);
    padding: 6px 12px;
  }

  .legend__dot {
    width: 8px;
    height: 8px;
    background: var(--lc, var(--ink-3));
  }

  .legend__chip[data-type='concept'] { --lc: var(--ph-concept); }
  .legend__chip[data-type='assets'] { --lc: var(--ph-assets); }
  .legend__chip[data-type='dev'] { --lc: var(--ph-dev); }
  .legend__chip[data-type='polish'] { --lc: var(--ph-polish); }
  .legend__chip[data-type='mkt'] { --lc: var(--ph-mkt); }
  .legend__chip[data-type='rel'] { --lc: var(--ph-rel); }
  .legend__chip[data-type='buf'] { --lc: var(--ph-buf); }
  .legend__chip[data-type='checkpoint'] { --lc: var(--accent); }

  /* sections */
  .rm-sec {
    margin-top: clamp(56px, 8vw, 88px);
  }

  .rm-sec__head {
    display: flex;
    align-items: baseline;
    gap: 18px;
    border-bottom: 1px solid var(--line-strong);
    padding-bottom: 14px;
    margin-bottom: 28px;
  }

  .rm-sec__idx {
    font-size: 12px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--ink-3);
    white-space: nowrap;
  }

  .rm-sec h2 {
    font-size: clamp(22px, 3.4vw, 34px);
  }

  .rm-sec h2 em {
    font-style: normal;
    color: var(--accent-ink);
  }

  /* flow */
  .flow {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(6, minmax(0, 1fr));
    gap: 12px;
  }

  .flow__step {
    border: 1px solid var(--line);
    background: var(--surface);
    padding: 18px 14px;
    text-align: center;
    display: flex;
    flex-direction: column;
    gap: 8px;
    align-items: center;
  }

  .flow__num {
    width: 30px;
    height: 30px;
    display: grid;
    place-items: center;
    background: var(--accent);
    color: var(--accent-contrast);
    font-weight: 700;
  }

  .flow__lbl {
    font-weight: 700;
    font-size: 14px;
    line-height: 1.3;
  }

  .flow__desc {
    font-size: 12.5px;
    color: var(--ink-2);
    line-height: 1.45;
  }

  @media (max-width: 1000px) {
    .flow {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 560px) {
    .flow {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .ios-note {
    display: flex;
    align-items: center;
    gap: 14px;
    border: 1px dashed var(--line-strong);
    padding: 14px 18px;
    margin-top: 18px;
    font-size: 14.5px;
    color: var(--ink-2);
  }

  .ios-note strong {
    color: var(--ink);
  }

  .ios-note__badge {
    background: var(--ph-polish);
    color: var(--paper);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.1em;
    padding: 4px 10px;
    white-space: nowrap;
  }

  /* rules */
  .rules {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 14px;
  }

  .rule {
    border: 1px solid var(--line);
    background: var(--surface);
    padding: 20px 22px;
  }

  .rule__num {
    font-size: 11px;
    letter-spacing: 0.18em;
    color: var(--accent-ink);
  }

  .rule h3 {
    font-size: 17px;
    margin-top: 10px;
  }

  .rule p {
    font-size: 14px;
    color: var(--ink-2);
    line-height: 1.6;
    margin-top: 8px;
  }

  @media (max-width: 900px) {
    .rules {
      grid-template-columns: 1fr;
    }
  }

  .rm-footer {
    margin-top: clamp(48px, 7vw, 72px);
    padding-top: 24px;
    border-top: 1px solid var(--line);
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 10px;
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ink-3);
  }

  .rm-footer__sig {
    text-transform: none;
    letter-spacing: 0.04em;
  }
</style>

<script>
  // Bilingual toggle: every translatable node carries data-ru/data-en.
  type Lang = 'ru' | 'en';
  const KEY = 'roadmap_lang';

  function apply(lang: Lang) {
    document.documentElement.lang = lang;
    document.querySelectorAll<HTMLElement>('[data-ru]').forEach((el) => {
      const text = el.dataset[lang];
      if (text !== undefined) el.textContent = text;
    });
    document.querySelectorAll<HTMLButtonElement>('[data-lang]').forEach((b) => {
      const active = b.dataset.lang === lang;
      b.classList.toggle('is-active', active);
      b.setAttribute('aria-pressed', String(active));
    });
    document.title =
      lang === 'ru' ? 'Дорожная карта · Taratorkin Official' : 'Sprint roadmap · Taratorkin Official';
  }

  function detect(): Lang {
    const url = new URLSearchParams(location.search).get('lang');
    if (url === 'ru' || url === 'en') return url;
    try {
      const saved = localStorage.getItem(KEY);
      if (saved === 'ru' || saved === 'en') return saved;
    } catch {}
    return (navigator.language || '').toLowerCase().startsWith('ru') ? 'ru' : 'en';
  }

  function init() {
    if (!document.querySelector('[data-lang]')) return;
    apply(detect());
    document.querySelectorAll<HTMLButtonElement>('[data-lang]').forEach((b) => {
      b.addEventListener('click', () => {
        const lang = b.dataset.lang as Lang;
        try {
          localStorage.setItem(KEY, lang);
        } catch {}
        apply(lang);
      });
    });
  }

  document.addEventListener('astro:page-load', init);
</script>
