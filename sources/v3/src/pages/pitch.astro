---
import { Image } from 'astro:assets';
import Base from '../layouts/Base.astro';
import GameCard from '../components/GameCard.astro';
import { GAMES, GROUPS } from '../data/games';
import { TEAM } from '../data/team';
import { SITE } from '../data/site';
import {
  PITCH_METRICS,
  PITCH_CALLOUT,
  ENGAGEMENT,
  SPRINT_FORMATS,
  TIMELINE,
  TIMELINE_NOTE,
  DELIVERABLES,
  STRENGTHS,
} from '../data/pitch';

const AGENDA = [
  { id: 'about', num: '01', label: 'Who we are' },
  { id: 'format', num: '02', label: 'How we work' },
  { id: 'timeline', num: '03', label: '20 working days' },
  { id: 'deliverables', num: '04', label: 'Deliverables' },
  { id: 'team', num: '05', label: 'The team' },
  { id: 'track', num: '06', label: 'Track record' },
  { id: 'strengths', num: '07', label: 'Why us' },
];

const rawBase = import.meta.env.BASE_URL;
const base = rawBase.endsWith('/') ? rawBase : rawBase + '/';

const collabGames = GAMES.filter((g) => g.group === 'collab');
const studioGames = GAMES.filter((g) => g.group === 'studio');
---

<Base
  title="Publisher pitch — Taratorkin Official"
  description="4-week sprints across hyper, hybrid, and casual. Full-stack team of 4, AI-accelerated iteration, dual-platform builds, founder with first-hand CrazyLabs pipeline experience."
  path="/pitch"
>
  <!-- ===== PITCH HERO ===== -->
  <section class="phero">
    <div class="container">
      <p class="eyebrow eyebrow--accent">Publisher pitch</p>
      <h1 class="phero__title display">
        Mobile games that ship <em>fast</em>. And perform.
      </h1>
      <p class="phero__lede">
        Taratorkin Official is a lean, founder-led studio building polished prototypes across
        <strong>hyper, hybrid, and casual</strong> for publishers. 4-week sprints. Full-stack
        team. Proven metrics.
      </p>
      <dl class="phero__metrics">
        {
          PITCH_METRICS.map((m) => (
            <div class="phero__metric">
              <dt>{m.label}</dt>
              <dd class="mono">{m.value}</dd>
            </div>
          ))
        }
      </dl>
    </div>
  </section>

  <div class="pitch container">
    <!-- sticky agenda -->
    <nav class="agenda glass" aria-label="Pitch sections">
      <p class="eyebrow">Agenda</p>
      <ol>
        {
          AGENDA.map((a) => (
            <li>
              <a href={`#${a.id}`} data-agenda-link>
                <span class="mono">{a.num}</span> {a.label}
              </a>
            </li>
          ))
        }
      </ol>
      <a href={`mailto:${SITE.email}`} class="btn btn--accent agenda__cta">Email us</a>
    </nav>

    <div class="pitch__content">
      <!-- 01 who we are -->
      <section class="psec" id="about">
        <h2 class="psec__title"><span class="mono">01</span> Who we are</h2>
        <p class="callout">{PITCH_CALLOUT}</p>
      </section>

      <!-- 02 how we work -->
      <section class="psec" id="format">
        <h2 class="psec__title"><span class="mono">02</span> How we work</h2>
        <p class="psec__lede">{ENGAGEMENT}</p>

        <div class="sprints">
          {
            SPRINT_FORMATS.map((f) => (
              <article class:list={['sprint', { 'sprint--preferred': f.preferred }]}>
                <p class="sprint__badge">{f.badge}</p>
                <p class="sprint__duration display">{f.duration}</p>
                <p class="sprint__type">{f.type}</p>
                <ul>
                  {f.features.map((feat) => (
                    <li>{feat}</li>
                  ))}
                </ul>
              </article>
            ))
          }
        </div>
      </section>

      <!-- 03 timeline -->
      <section class="psec" id="timeline">
        <h2 class="psec__title"><span class="mono">03</span> 20 working days</h2>
        <p class="psec__lede">
          How a 4-week sprint breaks down — enforced by hard freezes, so week 4 stays reserved
          for release, not finishing.
        </p>

        <div class="weeks">
          {
            TIMELINE.map((w) => (
              <article class="week" style={`--wc: var(${w.colorVar})`}>
                <p class="week__num">{w.num} · {w.phase}</p>
                <h3 class="week__title">{w.title}</h3>
                <ul>
                  {w.items.map((item) => (
                    <li class:list={[{ milestone: item.milestone }]}>{item.text}</li>
                  ))}
                </ul>
              </article>
            ))
          }
        </div>
        <p class="note">🔒 {TIMELINE_NOTE}</p>
      </section>

      <!-- 04 deliverables -->
      <section class="psec" id="deliverables">
        <h2 class="psec__title"><span class="mono">04</span> Deliverables</h2>
        <p class="psec__lede">Every prototype includes a complete, publisher-ready package.</p>
        <ul class="deliver">
          {
            DELIVERABLES.map((d, i) => (
              <li class="deliver__item">
                <span class="mono deliver__num">{String(i + 1).padStart(2, '0')}</span>
                <div>
                  <h3>{d.title}</h3>
                  <p>{d.text}</p>
                </div>
              </li>
            ))
          }
        </ul>
      </section>

      <!-- 05 team -->
      <section class="psec" id="team">
        <h2 class="psec__title"><span class="mono">05</span> The team</h2>
        <p class="psec__lede">
          Development, art, level design, game design, and UA creatives — all under one roof.
        </p>
        <div class="pteam">
          {
            TEAM.map((m) => (
              <article class="pmember">
                <Image
                  src={m.photo}
                  alt={`Portrait of ${m.name}`}
                  width={240}
                  widths={[120, 240]}
                  sizes="120px"
                  class="pmember__photo"
                  loading="lazy"
                />
                <div>
                  <h3>{m.name}</h3>
                  <p class="pmember__role">{m.role}</p>
                  <p class="pmember__bio">{m.bio}</p>
                  <p class="pmember__links">
                    {m.links.map((l) => (
                      <a href={l.url} target="_blank" rel="noopener" class="link">
                        {l.label} ↗
                      </a>
                    ))}
                  </p>
                </div>
              </article>
            ))
          }
        </div>
      </section>

      <!-- 06 track record -->
      <section class="psec" id="track">
        <h2 class="psec__title"><span class="mono">06</span> Track record</h2>
        <p class="psec__lede">
          Live on Google Play and the App Store — partner collaborations and our own releases.
        </p>

        <h3 class="track-sub">{GROUPS.collab.label} · {GROUPS.collab.note}</h3>
        <div class="track-grid">
          {collabGames.map((game) => <GameCard game={game} />)}
        </div>

        <h3 class="track-sub">{GROUPS.studio.label} · {GROUPS.studio.note}</h3>
        <div class="track-grid">
          {studioGames.map((game) => <GameCard game={game} />)}
        </div>
      </section>

      <!-- 07 strengths -->
      <section class="psec" id="strengths">
        <h2 class="psec__title"><span class="mono">07</span> Why us</h2>
        <div class="strengths">
          {
            STRENGTHS.map((s, i) => (
              <article class="strength">
                <p class="mono strength__num">{String(i + 1).padStart(2, '0')}</p>
                <h3>{s.title}</h3>
                <p>{s.text}</p>
              </article>
            ))
          }
        </div>
      </section>

      <!-- CTA -->
      <section class="psec pcta">
        <h2 class="display">Let's build something together.</h2>
        <p>
          Engagement models, sprint formats, and timeline are detailed above. Drop us an email
          — let's talk specifics.
        </p>
        <div class="pcta__actions">
          <a href={`mailto:${SITE.email}`} class="btn btn--accent">{SITE.email}</a>
          <a href={base} class="btn">← Back to site</a>
        </div>
      </section>
    </div>
  </div>
</Base>

<style>
  .phero {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    border-bottom: 1px solid var(--line);
    padding-block: clamp(64px, 9vw, 120px);
  }

  .phero::before {
    content: '';
    position: absolute;
    z-index: -1;
    top: -30%;
    left: 8%;
    width: min(720px, 80vw);
    aspect-ratio: 1.6;
    background: radial-gradient(ellipse at center, var(--glow), transparent 70%);
    filter: blur(40px);
    pointer-events: none;
  }

  .phero__title {
    font-size: clamp(32px, 5.6vw, 72px);
    max-width: 20ch;
    margin-top: 24px;
  }

  .phero__title em {
    font-style: normal;
    background: linear-gradient(100deg, var(--accent), var(--accent-2));
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .phero__lede {
    max-width: 58ch;
    font-size: clamp(16px, 1.5vw, 19px);
    color: var(--text-2);
    margin-top: 24px;
  }

  .phero__metrics {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 14px;
    margin-top: 44px;
    max-width: 760px;
  }

  .phero__metric {
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding: 20px;
    background: var(--glass);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
  }

  .phero__metric dt {
    font-size: 12px;
    font-weight: 600;
    color: var(--text-2);
    order: 2;
  }

  .phero__metric dd {
    font-size: clamp(20px, 2.4vw, 30px);
    font-weight: 700;
    background: linear-gradient(100deg, var(--accent), var(--accent-2));
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  @media (max-width: 720px) {
    .phero__metrics {
      grid-template-columns: repeat(2, 1fr);
      gap: 10px;
    }
  }

  /* ===== two-column pitch layout ===== */
  .pitch {
    display: grid;
    grid-template-columns: 240px minmax(0, 1fr);
    gap: clamp(32px, 5vw, 80px);
    align-items: start;
    padding-block: clamp(48px, 6vw, 88px);
  }

  .agenda {
    position: sticky;
    top: calc(var(--header-h) + 24px);
    padding: 20px;
    border-radius: var(--radius-lg);
  }

  .agenda ol {
    list-style: none;
    margin-top: 14px;
  }

  .agenda a {
    display: flex;
    gap: 10px;
    padding: 9px 2px;
    font-size: 14px;
    text-decoration: none;
    color: var(--text-2);
    border-radius: 8px;
    transition: color 0.15s ease;
  }

  .agenda a:hover {
    color: var(--text);
  }

  .agenda a.is-active {
    color: var(--accent-ink);
    font-weight: 650;
  }

  .agenda a .mono {
    font-size: 11px;
    padding-top: 2px;
    color: var(--text-3);
  }

  .agenda__cta {
    margin-top: 20px;
    width: 100%;
    justify-content: center;
  }

  @media (max-width: 900px) {
    .pitch {
      grid-template-columns: 1fr;
    }

    .agenda {
      position: static;
    }

    .agenda ol {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
      gap: 4px;
    }

    .agenda__cta {
      width: auto;
    }
  }

  /* ===== sections ===== */
  .psec {
    padding-block: clamp(36px, 4.5vw, 64px);
    border-top: 1px solid var(--line);
  }

  .psec:first-child {
    border-top: none;
    padding-top: 0;
  }

  .psec__title {
    font-family: var(--font-display);
    font-size: clamp(22px, 3vw, 34px);
    font-weight: 600;
    display: flex;
    gap: 14px;
    align-items: baseline;
    scroll-margin-top: calc(var(--header-h) + 24px);
  }

  .psec__title .mono {
    font-size: 13px;
    color: var(--accent-ink);
  }

  .psec__lede {
    max-width: 64ch;
    color: var(--text-2);
    margin-top: 14px;
  }

  .callout {
    position: relative;
    overflow: hidden;
    background: var(--glass);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    padding: 24px 28px 24px 31px;
    line-height: 1.8;
    color: var(--text-2);
    margin-top: 22px;
  }

  .callout::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 3px;
    background: linear-gradient(var(--accent), var(--accent-2));
  }

  /* sprints */
  .sprints {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 18px;
    margin-top: 30px;
  }

  .sprint {
    background: var(--glass);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    padding: 26px;
  }

  .sprint--preferred {
    border: 1px solid transparent;
    background:
      linear-gradient(var(--bg-2), var(--bg-2)) padding-box,
      linear-gradient(135deg, var(--accent), var(--accent-2)) border-box;
    box-shadow: 0 14px 40px -14px var(--glow);
  }

  .sprint__badge {
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.08em;
    color: var(--accent-ink);
  }

  .sprint__duration {
    font-size: clamp(38px, 5vw, 58px);
    margin-top: 10px;
    line-height: 1.1;
    background: linear-gradient(100deg, var(--accent), var(--accent-2));
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .sprint__type {
    font-size: 14px;
    color: var(--text-2);
    margin-top: 6px;
  }

  .sprint ul {
    list-style: none;
    margin-top: 20px;
    display: grid;
    gap: 9px;
  }

  .sprint li {
    font-size: 14.5px;
    color: var(--text-2);
    padding-left: 18px;
    position: relative;
  }

  .sprint li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 9px;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--accent);
  }

  @media (max-width: 720px) {
    .sprints {
      grid-template-columns: 1fr;
    }
  }

  /* timeline weeks */
  .weeks {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 14px;
    margin-top: 30px;
  }

  .week {
    position: relative;
    background: var(--glass);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    padding: 22px 20px 20px;
  }

  .week::before {
    content: '';
    position: absolute;
    top: 0;
    left: 18px;
    right: 18px;
    height: 3px;
    border-radius: 0 0 3px 3px;
    background: var(--wc);
  }

  .week__num {
    font-size: 12px;
    font-weight: 650;
    letter-spacing: 0.05em;
    color: var(--wc);
  }

  .week__title {
    font-size: 16px;
    margin-top: 8px;
  }

  .week ul {
    list-style: none;
    margin-top: 14px;
    display: grid;
    gap: 8px;
  }

  .week li {
    font-size: 13.5px;
    color: var(--text-2);
    padding-left: 15px;
    position: relative;
  }

  .week li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 8px;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--wc);
    opacity: 0.5;
  }

  .week li.milestone {
    color: var(--text);
    font-weight: 650;
  }

  .week li.milestone::before {
    opacity: 1;
    box-shadow: 0 0 10px var(--wc);
  }

  .note {
    margin-top: 18px;
    background: var(--glass);
    border: 1px dashed var(--line-2);
    border-radius: var(--radius-lg);
    padding: 14px 22px;
    font-size: 14px;
    color: var(--text-2);
  }

  @media (max-width: 900px) {
    .weeks {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 560px) {
    .weeks {
      grid-template-columns: 1fr;
    }
  }

  /* deliverables */
  .deliver {
    list-style: none;
    margin-top: 26px;
    display: grid;
    gap: 12px;
  }

  .deliver__item {
    display: flex;
    gap: 22px;
    padding: 18px 20px;
    background: var(--glass);
    border: 1px solid var(--line);
    border-radius: var(--radius-sm);
    box-shadow: var(--shadow);
  }

  .deliver__num {
    font-size: 13px;
    padding-top: 4px;
    background: linear-gradient(100deg, var(--accent), var(--accent-2));
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .deliver__item h3 {
    font-size: 16px;
  }

  .deliver__item p {
    font-size: 14.5px;
    color: var(--text-2);
    margin-top: 4px;
    max-width: 70ch;
  }

  /* team */
  .pteam {
    display: grid;
    gap: 16px;
    margin-top: 28px;
  }

  .pmember {
    display: grid;
    grid-template-columns: 120px minmax(0, 1fr);
    gap: 22px;
    background: var(--glass);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    padding: 20px;
  }

  .pmember :global(.pmember__photo) {
    width: 120px;
    height: 120px;
    object-fit: cover;
    border-radius: 20px;
    border: 1px solid var(--line-2);
  }

  .pmember h3 {
    font-size: 17px;
  }

  .pmember__role {
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.06em;
    color: var(--accent-ink);
    margin-top: 4px;
  }

  .pmember__bio {
    font-size: 14px;
    line-height: 1.65;
    color: var(--text-2);
    margin-top: 10px;
  }

  .pmember__links {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    margin-top: 12px;
  }

  @media (max-width: 560px) {
    .pmember {
      grid-template-columns: 1fr;
    }
  }

  /* track record */
  .track-sub {
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.08em;
    color: var(--text-3);
    margin: 36px 0 16px;
  }

  .track-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: 16px;
  }

  /* strengths */
  .strengths {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 16px;
    margin-top: 26px;
  }

  .strength {
    background: var(--glass);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    padding: 22px;
  }

  .strength__num {
    font-size: 12px;
    background: linear-gradient(100deg, var(--accent), var(--accent-2));
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .strength h3 {
    font-size: 16px;
    margin-top: 10px;
  }

  .strength p {
    font-size: 14px;
    color: var(--text-2);
    margin-top: 8px;
    line-height: 1.65;
  }

  @media (max-width: 720px) {
    .strengths {
      grid-template-columns: 1fr;
    }
  }

  /* cta */
  .pcta {
    text-align: left;
    position: relative;
    isolation: isolate;
    overflow: hidden;
    margin-top: clamp(36px, 4.5vw, 64px);
    padding: clamp(32px, 4.5vw, 56px);
    border: 1px solid var(--line-2);
    border-radius: var(--radius-lg);
    background: linear-gradient(
      135deg,
      color-mix(in srgb, var(--accent) 24%, var(--bg-2)),
      color-mix(in srgb, var(--accent-2) 18%, var(--bg-2))
    );
    box-shadow: 0 20px 60px -20px var(--glow);
  }

  .pcta::before {
    content: '';
    position: absolute;
    z-index: -1;
    inset: 0;
    background: radial-gradient(ellipse at 18% 0%, var(--glow), transparent 60%);
    pointer-events: none;
  }

  .pcta h2 {
    font-size: clamp(26px, 4vw, 44px);
  }

  .pcta p {
    color: var(--text-2);
    margin-top: 14px;
    max-width: 50ch;
  }

  .pcta__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 28px;
  }

  /* ===== print — publishers save this as PDF ===== */
  @media print {
    .agenda,
    :global(header),
    :global(footer) {
      display: none !important;
    }

    .pitch {
      display: block;
    }

    .psec {
      break-inside: avoid-page;
    }

    .phero,
    .phero__metric,
    .callout,
    .sprint,
    .sprint--preferred,
    .week,
    .note,
    .deliver__item,
    .pmember,
    .strength,
    .pcta {
      background: #fff !important;
      color: #000 !important;
      box-shadow: none !important;
      backdrop-filter: none !important;
      -webkit-backdrop-filter: none !important;
      border-color: #bbb !important;
    }

    .phero::before,
    .pcta::before {
      display: none !important;
    }

    .sprint--preferred {
      outline: 2px solid #000;
    }

    .phero__title em,
    .phero__metric dd,
    .sprint__duration,
    .deliver__num,
    .strength__num {
      background: none !important;
      -webkit-text-fill-color: #000 !important;
      color: #000 !important;
    }

    .phero__lede,
    .psec__lede,
    .callout,
    .sprint li,
    .sprint__type,
    .week li,
    .note,
    .deliver__item p,
    .pmember__bio,
    .strength p,
    .pcta p {
      color: #000 !important;
    }
  }
</style>

<script>
  // Scrollspy for the agenda nav.
  function initScrollspy() {
    const nav = document.querySelector<HTMLElement>('.agenda');
    if (!nav || nav.hasAttribute('data-spy-ready')) return;
    nav.setAttribute('data-spy-ready', '');
    const links = document.querySelectorAll<HTMLAnchorElement>('[data-agenda-link]');
    if (!links.length) return;
    const map = new Map<Element, HTMLAnchorElement>();
    for (const link of links) {
      const id = link.getAttribute('href')?.slice(1);
      const sec = id ? document.getElementById(id) : null;
      if (sec) map.set(sec, link);
    }
    const io = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          const link = map.get(entry.target);
          if (!link) continue;
          if (entry.isIntersecting) {
            links.forEach((l) => l.classList.remove('is-active'));
            link.classList.add('is-active');
          }
        }
      },
      { rootMargin: '-20% 0px -70% 0px' },
    );
    map.forEach((_, sec) => io.observe(sec));
  }

  // Run immediately too: on a first client-side (View Transitions) visit this
  // module can finish loading AFTER astro:page-load has already fired.
  initScrollspy();
  document.addEventListener('astro:page-load', initScrollspy);
</script>
